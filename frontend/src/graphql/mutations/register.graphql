mutation Register($name: String!, $password: String!) {
  registerStudent(options: { name: $name, password: $password }) {
    errors {
      field
      message
    }
    student {
      id
      name
    }
  }
}
